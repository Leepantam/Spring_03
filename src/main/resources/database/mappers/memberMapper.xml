<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
 <mapper namespace="com.leepantam.s3.member.MemberDAO">
 	<select id="memberLogin" parameterType="com.leepantam.s3.member.MemberDTO" resultType="com.leepantam.s3.member.MemberDTO">
 		select * from users where id=#{id} and pw=#{pw}
 	</select>
 	
 	<insert id="memberJoin" parameterType="com.leepantam.s3.member.MemberDTO">
 		insert into users (id,pw,name,phone,email) values(#{id},#{pw},#{name},#{phone},#{email})
 	</insert>
 	
 	<delete id="memberDelete" parameterType="com.leepantam.s3.member.MemberDTO">
 		delete from users where id=#{id}
 	</delete>
 
 
 </mapper>