<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
 <mapper namespace="com.leepantam.s3.MemberDAO">
 	<select id="memberLogin" parameterType="com.leepantam.s3.member.MemberDTO" resultType="">
 		select * from users where id=#{id} and pw=#{pw}
 	</select>
 
 
 </mapper>